// * 
function isPassed(grade){
    return grade>=3;
}
function isValid(password){
    let isValid=true;
    if(password>8){

    }
}

//Pure functions - allays returns the same result when the input is the same
// function add(a,b)-> return a+b